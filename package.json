{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "studio": "cd apps/backend && pnpm run studio",
    "vercel-build": "turbo build",
    "deploy": "vercel",
    "deploy:prod": "vercel --prod"
  },
  "dependencies": {
    "@oslojs/crypto": "^1.0.1",
    "@oslojs/encoding": "^1.1.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "vercel": "^24.0.0"
  },
  "packageManager": "pnpm@8.15.6",
  "name": "cartverse",
  "engines": {
    "node": "22.x"
  }
}
